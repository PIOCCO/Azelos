# Fine-tuning (optional, separate module)

**RAG** = company knowledge at query time.  
**Fine-tuning** = adapt model behavior/style — only when a concrete use case exists.

Do not mix fine-tuning jobs into the default ingestion pipeline.
