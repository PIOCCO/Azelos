# Operations

```bash
./scripts/ai-blueprint status
./scripts/ai-blueprint health
./scripts/ai-blueprint backup
./scripts/ai-blueprint reindex <document_id>
```

Monitor `/ready`, worker queue depth, indexing failures in admin UI.
