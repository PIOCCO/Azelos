# Deploy the built app under a subdirectory (must end with / for Vite)
# Example: upload dist/ contents to https://your-domain.com/AIPO/
VITE_BASE_PATH=/AIPO/

# Optional: absolute API origin if /api is not on the same host as the static files
# VITE_API_URL=https://api.example.com
