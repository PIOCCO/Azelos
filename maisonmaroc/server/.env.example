# Server
PORT=3001
NODE_ENV=development

# Frontend origin (comma-separated for multiple)
CLIENT_ORIGIN=http://localhost:5173

# JWT (use a long random string in production)
JWT_SECRET=change-me-in-production-use-openssl-rand-hex-32
JWT_EXPIRES_IN=7d

# Bootstrap super-admin (created once if no SUPER_ADMIN exists)
SUPER_ADMIN_EMAIL=admin@apio.ma
SUPER_ADMIN_PASSWORD=change-me-on-first-login
SUPER_ADMIN_NAME=Super Admin

# Optional demo owner (linked to seed agent ahmed-el-amrani)
DEMO_OWNER_EMAIL=owner.demo@apio.ma
DEMO_OWNER_PASSWORD=change-me-demo-owner

# Google OAuth — CLIENT accounts only
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3001/api/auth/google/callback

# Where to send the browser after successful Google OAuth
FRONTEND_URL=http://localhost:5173

# Cookie settings (production: Secure=true, SameSite=lax or strict)
COOKIE_SECURE=false
COOKIE_SAME_SITE=lax
