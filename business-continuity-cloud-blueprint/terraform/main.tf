# Business Continuity Cloud Blueprint — Terraform entry point
#
# Deploy from an environment directory (recommended):
#   cd terraform/environments/dev
#   cp terraform.tfvars.example terraform.tfvars
#   terraform init && terraform plan && terraform apply
#
# Environment roots compose modules under terraform/modules/.
