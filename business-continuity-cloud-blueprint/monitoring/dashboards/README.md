# Dashboards

Import Azure Monitor workbook templates during client onboarding. Wire panels to:

- App Service availability
- PostgreSQL CPU/connections/storage
- Backup job success rate
- Recovery vault health

Store exported workbook JSON per client under a private artifact store — not in Git if client-specific.
