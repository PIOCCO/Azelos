# Control framework

Controls are defined in `config/controls.yaml` with:

- TSC mapping (`security`, `availability`, etc.)
- Evidence types
- Optional `automated_check` id resolved in `automation/soc2/checks.py`

Extend the catalog per client industry; do not remove Security CC controls without assessor agreement.
