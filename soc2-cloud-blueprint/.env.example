# Local / CI — never commit production secrets

SOC2BP_ROOT=
SOC2BP_CLIENT_CONFIG=config/clients/example.yaml
SOC2BP_ENV=development

AZURE_SUBSCRIPTION_ID=
AZURE_TENANT_ID=

# Mock evidence collection without cloud API calls
SOC2BP_MOCK_MODE=false
