# Azure collectors

Evidence automation uses `az` CLI when `SOC2BP_MOCK_MODE=false`:

- Account metadata
- Extend with Policy compliance export, Defender recommendations, Activity Log diagnostics

Map results to controls in `config/controls.yaml`.
